import PageStub from '../../components/common/PageStub.jsx';

export default function NotificationsFeed() {
  return (
    <PageStub
      title="Notifications"
      description="Live feed from GET /api/notifications, via Firestore onSnapshot."
    />
  );
}
