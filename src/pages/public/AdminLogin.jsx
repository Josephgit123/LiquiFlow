import PageStub from '../../components/common/PageStub.jsx';

export default function AdminLogin() {
  return (
    <PageStub
      title="Admin Login"
      description="Access ID / access token login, separate from merchant Firebase Auth."
    />
  );
}
