import PageStub from '../../components/common/PageStub.jsx';

export default function RefundQueue() {
  return (
    <PageStub
      title="Refund Queue"
      description="Flagged transactions from GET /api/admin/refunds/queue — currently a labeled placeholder criterion pending a PENDING_APPROVAL decision (Step 15)."
    />
  );
}
