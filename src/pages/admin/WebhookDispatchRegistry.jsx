import PageStub from '../../components/common/PageStub.jsx';

export default function WebhookDispatchRegistry() {
  return (
    <PageStub
      title="Webhook Dispatch Registry"
      description="Platform-wide view of registered webhook subscriptions and delivery status."
    />
  );
}
